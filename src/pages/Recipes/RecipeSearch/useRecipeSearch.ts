const useRecipeSearch = () => {
  const handleSearch = () => {
    return;
  };
  const handleChange = () => {
    return;
  };
  return { handleSearch, handleChange };
};

export default useRecipeSearch;
